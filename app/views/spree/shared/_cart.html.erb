<%= link_to spree.cart_path,
  class: "cart-icon cart-icon--visible-count #{local_assigns[:class]}",
  'aria-label': Spree.t(:cart) do %>
  <%= icon(name: 'cart',
           width: size,
           height: size) %>

  <div class="font-weight-medium cart-icon-count"><%= current_order.item_count %></div>
<% end %>
